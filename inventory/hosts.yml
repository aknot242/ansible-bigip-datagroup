all:
  children:
    big_ips:
      hosts:
        yourhostnamehere:
            ansible_connection: local
            bigip_provider:
              server: "{{ inventory_hostname }}"
              user: admin
              password: yourpasswordhere
              validate_certs: false
              server_port: 443